import { useNavigate } from "react-router-dom";
import appImages from "../../assets";

const HeaderWindow = () => {
  const navigate = useNavigate();
  return <div className="headerWinCont"></div>;
};

export default HeaderWindow;
